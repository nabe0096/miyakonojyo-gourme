# 都城グルメ応援プロジェクト（mj-gourmet.jp）

## サイト情報

| 項目 | 内容 |
|---|---|
| 公開URL | https://mj-gourmet.jp/ |
| GitHub | https://github.com/nabe0096/miyakonojyo-gourme |
| ホスティング | GitHub Pages（`CNAME` で独自ドメイン） |
| デプロイ | `bash deploy.sh`（画像を自動圧縮してからpush） |
| 店舗個別URL | `https://mj-gourmet.jp/?store={data-id}` |

**このフォルダの写真は消さない。**サイトが直接読みに行くため、消すとギャラリーがリンク切れになる。
容量の大半は `.git`（約683MB）で、写真本体は数十MB。写真を消しても容量はほぼ減らない（2026-08-20の教訓）。

---

# 【正典】新しい店舗に行ったときのワークフロー

社長から店舗情報が来たら、**この順番で最後まで通す**。各ステップの完了を報告してから次へ進む。

## 社長からもらう情報（足りなければ必ず聞く）

1. **店舗情報**（店名・住所・営業時間・定休日・電話番号・Instagram）
2. **感想コメント**（本文そのまま使う。創作しない）
3. **訪問日**
4. **YouTubeショートのURL**
5. **写真フォルダ**（`行ったお店/{店名}/photo/` に配置済みか確認）
6. **コメントの署名**（`Comment by ◯◯`）→ **毎回必ず聞く。nabe / keiko など担当者が変わる**
7. **Googleドライブの共有リンク**（店舗へ送る素材）

> 揃っていない項目があれば、勝手に埋めずに社長へ確認する。特に**電話番号と署名は聞き漏らしやすい**。

---

## STEP 1：YouTube概要欄を作る

`_テンプレート/YouTube概要欄.md` の型に沿って作る。**構造は変えない。**

- 本文は社長の感想コメントをそのまま使う（要約・言い換えをしない）
- 店舗情報ブロックは `━━━` の罫線で挟む
- 「📸今回撮影した写真はこちら」の下に**店舗個別ページのURL**を入れる
- **ハッシュタグは業態に合わせて毎回組み替える。** カフェの店に行ったときのタグを使い回さない
  - 固定：`#都城グルメ #宮崎グルメ #都城ランチ #宮崎ランチ #都城グルメ応援プロジェクト`
  - 業態別で足す：ハンバーガー→`#都城バーガー #ハンバーガー`／カフェ→`#都城カフェ #宮崎カフェ #カフェ巡り` など
  - **2026-08-20に、バーガー店の概要欄へカフェ系タグが残っていた実例あり。必ず見直す**

## STEP 2：サイトに店舗カードを追加

`index.html` の店舗ギャラリー（`.store__card` が並ぶ場所）に、**既存カードをコピーして**追加する。

- 挿入位置：最後のカードの `</article>` の直後、`</div>` の前
- `data-id` は英小文字（例：`burgerhouseu`）。**これが個別ページURLになる**
- `data-images` の**先頭にYouTubeショートのURL**、その後に写真を全部入れる
- 表紙は「料理のメイン1枚＋サムネ3枚」。サムネは**料理・調理風景・外観**から1枚ずつ選ぶと良い
- `data-memo` の末尾に `Comment by ◯◯`（STEP 0で聞いた署名）
- `store__badge` は訪問月（例：`2026.08`）

## STEP 3：動画セクションに追加

`.reel__frame` の中に `.reel__video` を1つ足す。

```html
<div class="reel__video">
  <iframe src="https://www.youtube.com/embed/{動画ID}" title="{店名} 撮影の様子" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
</div>
```

**動画が4本以上になったらレイアウトを見直す。** 現在の設定（`style.css`）：
PC 3列 / 900px以下 2列 / 760px以下 1列。4本目を足すと2段になるので、その時点で社長に相談する。

## STEP 4：検品（1つでも欠けたらデプロイしない）

- [ ] 店舗カードの数が想定どおりか（`data-store` を数える）
- [ ] `data-images` の枚数＝写真フォルダの枚数＋動画1
- [ ] **全画像が実際に読み込めるか**（HTTPステータス200を1枚ずつ確認）
- [ ] 表示項目が既存店舗と揃っているか（店名・訪問日・住所・電話・営業時間・Instagram）
- [ ] `Comment by ◯◯` の署名が正しいか
- [ ] 個別ページURL `?store={data-id}` が開くか

## STEP 5：デプロイと本番確認

```bash
bash deploy.sh
# GitHub Pagesのビルド完了を待つ
gh api repos/nabe0096/miyakonojyo-gourme/pages/builds/latest --jq .status   # built になるまで
```

**本番URLで再度、写真の読み込みと表示項目を確認してから完了報告する。**

## STEP 6：店舗へ送る文面を作る

`_テンプレート/店舗への納品メール.md` を使う。差し替えるのは2箇所だけ。

1. GoogleドライブのリンクM
2. 店舗個別ページURL（`?store={data-id}`）

## STEP 7：Instagram用のメッセージを作る

`_テンプレート/店舗へのInstagramメッセージ.md` を使う。メールより短く、DMで読みやすい長さにする。

---

## 参考：完了した店舗

| 店舗 | data-id | 訪問 | 署名 |
|---|---|---|---|
| 茶房KUMU（くむ） | `kumu` | 2026-07-03 | ― |
| 自由料理屋 て | `te` | 2026-07-18 | nabe |
| BURGER HOUSE U | `burgerhouseu` | 2026-08-04 | keiko |
